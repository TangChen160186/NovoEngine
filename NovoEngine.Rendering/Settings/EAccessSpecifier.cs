namespace OvRenderingCS.Settings
{
    /// <summary>
    /// Defines how a buffer can be accessed
    /// </summary>
    public enum EAccessSpecifier
    {
        /// <summary>
        /// Read only access
        /// </summary>
        ReadOnly,

        /// <summary>
        /// Write only access
        /// </summary>
        WriteOnly,

        /// <summary>
        /// Read and write access
        /// </summary>
        ReadWrite
    }
}
